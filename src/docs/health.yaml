paths:
  /health:
    get:
      tags:
        - Health
      summary: Health Check
      responses:
        '200':
          description: Returns a message with status code 200
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: "OK"