import { NextFunction, Request, RequestHandler, Response } from "express";

import { AuthenticatedRequest } from "../middlewares/user";
// import User, { Address, UserRoles, userUpdateSchema } from "../models/user";